---
title: JSON Web Token
sidebar_position: 4
---

if (TextDecoder == null) {
  TextDecoder = require('util').TextDecoder 
}

if (TextEncoder == null) {
  TextEncoder = require('util').TextEncoder 
}


import {Jwt} from '@local/components/jwt'

## Sandbox

<Jwt/>
